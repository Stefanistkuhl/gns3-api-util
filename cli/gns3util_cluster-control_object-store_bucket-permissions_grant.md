## gns3util cluster-control object-store bucket-permissions grant

Grant a permission on a bucket

```
gns3util cluster-control object-store bucket-permissions grant <bucket-id> [flags]
```

### Examples

```
  gns3util object-store bucket-permissions grant <bucket-id> \
    --principal-type user --principal-id user123 --permission read
```

### Options

```
      --expires-at string       Optional expiry timestamp (RFC3339)
  -h, --help                    help for grant
      --permission string       Permission level: read, write, or admin (required)
      --principal-id string     ID of the user/group/role to grant access to (required)
      --principal-type string   Principal type: user, group, or role (required)
```

### Options inherited from parent commands

```
  -c, --cluster string      Cluster name from keyfile. Mutually exclusive with --server
  -i, --insecure            Ignore unsigned SSL-Certificates. Can be set via GNS3_INSECURE
  -k, --key-file string     Path to authentication keyfile. Can be set via GNS3_KEY_FILE or config.toml
  -o, --output string       Output format: [json, json-colorless, collapsed, yaml, toml, table, table-ascii]. Can be set via GNS3_OUTPUT (default "table")
  -s, --server string       GNS3v3 Server URL. Can be set via GNS3_SERVER or config.toml
      --user string[="~"]   User to select from the keyfile. Omit the value to open a fuzzy picker (--user), or pass a name (--user alice).
```

### SEE ALSO

* [gns3util cluster-control object-store bucket-permissions](gns3util_cluster-control_object-store_bucket-permissions.md)	 - Manage access permissions on buckets

###### Auto generated by spf13/cobra on 9-May-2026
