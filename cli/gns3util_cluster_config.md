## gns3util cluster config

cluster config operations

### Synopsis

commands to manage your cluster config

```
gns3util cluster config [flags]
```

### Options

```
  -h, --help   help for config
```

### Options inherited from parent commands

```
  -c, --cluster string      Cluster name from keyfile. Mutually exclusive with --server
  -i, --insecure            Ignore unsigned SSL-Certificates. Can be set via GNS3_INSECURE
  -k, --key-file string     Path to authentication keyfile. Can be set via GNS3_KEY_FILE or config.toml
  -o, --output string       Output format: [json, json-colorless, collapsed, yaml, toml, table, table-ascii]. Can be set via GNS3_OUTPUT (default "table")
  -s, --server string       GNS3v3 Server URL. Can be set via GNS3_SERVER or config.toml
      --user string[="~"]   User to select from the keyfile. Omit the value to open a fuzzy picker (--user), or pass a name (--user alice).
```

### SEE ALSO

* [gns3util cluster](gns3util_cluster.md)	 - cluster operations
* [gns3util cluster config apply](gns3util_cluster_config_apply.md)	 - apply your config file to the local database
* [gns3util cluster config edit](gns3util_cluster_config_edit.md)	 - edit your configuration with your $EDITOR
* [gns3util cluster config purge](gns3util_cluster_config_purge.md)	 - purge the config file and the local database
* [gns3util cluster config sync](gns3util_cluster_config_sync.md)	 - sync your cluster config file with the local database

###### Auto generated by spf13/cobra on 9-May-2026
