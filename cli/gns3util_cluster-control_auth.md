## gns3util cluster-control auth

Authentication related operations for clusters

### Synopsis

Authentication related operations for cluster management.

```
gns3util cluster-control auth [flags]
```

### Options

```
  -h, --help   help for auth
```

### Options inherited from parent commands

```
  -c, --cluster string      Cluster name from keyfile. Mutually exclusive with --server
  -i, --insecure            Ignore unsigned SSL-Certificates. Can be set via GNS3_INSECURE
  -k, --key-file string     Path to authentication keyfile. Can be set via GNS3_KEY_FILE or config.toml
  -o, --output string       Output format: [json, json-colorless, collapsed, yaml, toml, table, table-ascii]. Can be set via GNS3_OUTPUT (default "table")
  -s, --server string       GNS3v3 Server URL. Can be set via GNS3_SERVER or config.toml
      --user string[="~"]   User to select from the keyfile. Omit the value to open a fuzzy picker (--user), or pass a name (--user alice).
```

### SEE ALSO

* [gns3util cluster-control](gns3util_cluster-control.md)	 - gns3util control plane operations
* [gns3util cluster-control auth add-user](gns3util_cluster-control_auth_add-user.md)	 - Store a user's JWT token for a cluster in the keyfile
* [gns3util cluster-control auth perms](gns3util_cluster-control_auth_perms.md)	 - Show the effective permissions for the current cluster auth context
* [gns3util cluster-control auth set-default-user](gns3util_cluster-control_auth_set-default-user.md)	 - Mark a stored user as the default for a cluster
* [gns3util cluster-control auth status](gns3util_cluster-control_auth_status.md)	 - Check authentication status against a cluster master

###### Auto generated by spf13/cobra on 9-May-2026
