## gns3util project duplicate

Duplicate a Project

### Synopsis

Duplicate a project on the GNS3 server.

```
gns3util project duplicate [project-name/id] [flags]
```

### Examples

```
gns3util -s https://controller:3080 project duplicate my-project --name "duplicated-project"
```

### Options

```
      --auto-close          Auto close the project
      --auto-open           Auto open the project
      --auto-start          Auto start the project
  -h, --help                help for duplicate
  -n, --name string         Name for the duplicated project (required)
      --path string         Path for the duplicated project
      --project-id string   Project ID for the duplicated project
      --scene-height int    Scene height
      --scene-width int     Scene width
      --show-grid           Show grid
      --show-layers         Show layers
      --snap-to-grid        Snap to grid
      --use-json string     Provide a raw JSON string to send instead of flags
      --zoom int            Zoom level
```

### Options inherited from parent commands

```
  -c, --cluster string      Cluster name from keyfile. Mutually exclusive with --server
  -i, --insecure            Ignore unsigned SSL-Certificates. Can be set via GNS3_INSECURE
  -k, --key-file string     Path to authentication keyfile. Can be set via GNS3_KEY_FILE or config.toml
  -o, --output string       Output format: [json, json-colorless, collapsed, yaml, toml, table, table-ascii]. Can be set via GNS3_OUTPUT (default "table")
  -s, --server string       GNS3v3 Server URL. Can be set via GNS3_SERVER or config.toml
      --user string[="~"]   User to select from the keyfile. Omit the value to open a fuzzy picker (--user), or pass a name (--user alice).
```

### SEE ALSO

* [gns3util project](gns3util_project.md)	 - Project operations

###### Auto generated by spf13/cobra on 9-May-2026
